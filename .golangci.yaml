version: "2"
linters:
  enable:
    - govet
    - unused
    - ineffassign
    - unconvert
    - unparam
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - vendor/
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/redhat-developer/gitops-backend
  exclusions:
    generated: lax
    paths:
      - vendor/
      - third_party$
      - builtin$
      - examples$